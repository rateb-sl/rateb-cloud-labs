# Projects

End-to-end applications belong here when they have an independent project identity. Keep application code and its infrastructure together when they describe one system.

A project earns its own repository only when it has an independent purpose, README, lifecycle, and audience. Small shared applications should remain inside the relevant project folder rather than being duplicated across repositories.
